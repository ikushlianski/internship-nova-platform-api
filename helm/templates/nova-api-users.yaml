{{- if .Values.users.create }}
{{- if .Values.serviceAccounts.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.users.name }}
  namespace: {{.Release.Namespace}}
  {{- with .Values.serviceAccounts.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.users.service.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    prometheus.io/scrape: "true"
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.users.name }}
  ports:
  - name: listener
    port: {{ .Values.users.service.port }}
    targetPort: {{ .Values.users.service.targetPort }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.users.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.users.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.users.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.users.name }}
    spec:
      {{- if .Values.serviceAccounts.create }}
      serviceAccountName: {{ .Values.users.name }}
      {{- else }}
      serviceAccountName: default
      {{- end }}
      # terminationGracePeriodSeconds: 5
      # securityContext:
      #   fsGroup: 1000
      #   runAsGroup: 1000
      #   runAsNonRoot: true
      #   runAsUser: 1000
      imagePullSecrets:
      - name: regcred
      initContainers:
      - name: wait-for-rabbitmq
        image: alpine
        {{- with (index .Values.rabbitmq.service.ports 0) }}
        command: ["/bin/sh", "-c", "until nc -zv -w1 {{ $.Values.rabbitmq.service.name }} {{ .port }}; do sleep 2; done"]
        {{- end }}
      containers:
      - name: {{ .Values.users.name }}
        # securityContext:
        #   runAsUser: 1000
        image: {{ .Values.images.repository }}/{{ .Values.users.image.name }}:{{ .Values.images.tag }}
        command: ["/bin/sh"]
        args: ["-c", "npx nest start users --watch"]
        ports:
        - containerPort: {{ .Values.users.service.targetPort }}
        envFrom:
          {{- toYaml .Values.envFrom | nindent 12 }}
        resources:
        #   {{- toYaml .Values.users.resources | nindent 10 }}
{{- end }}
