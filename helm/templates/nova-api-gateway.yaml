{{- if .Values.gateway.create }}
{{- if .Values.serviceAccounts.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.gateway.name }}
  namespace: {{.Release.Namespace}}
  {{- with .Values.serviceAccounts.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.gateway.service.name }}
  namespace: {{ .Release.Namespace }}
  annotations:
    prometheus.io/scrape: "true"
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.gateway.name }}
  ports:
  - name: listener
    port: {{ .Values.gateway.service.port }}
    targetPort: {{ .Values.gateway.service.targetPort }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.gateway.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.gateway.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.gateway.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.gateway.name }}
    spec:
      {{- if .Values.serviceAccounts.create }}
      serviceAccountName: {{ .Values.gateway.name }}
      {{- else }}
      serviceAccountName: default
      {{- end }}
      # terminationGracePeriodSeconds: 5
      # securityContext:
      #   fsGroup: 1000
      #   runAsGroup: 1000
      #   runAsNonRoot: true
      #   runAsUser: 1000
      imagePullSecrets:
      - name: regcred
      initContainers:
      - name: wait-for-rabbitmq
        image: alpine
        {{- with (index .Values.rabbitmq.service.ports 0) }}
        command: ["/bin/sh", "-c", "until nc -zv -w1 {{ $.Values.rabbitmq.service.name }} {{ .port }}; do sleep 2; done"]
        {{- end }}
      containers:
      - name: {{ .Values.gateway.name }}
        # securityContext:
        #   runAsUser: 1000
        image: {{ .Values.images.repository }}/{{ .Values.gateway.image.name }}:{{ .Values.images.tag }}
        command: ["/bin/sh"]
        args: ["-c", "npx prisma migrate dev && npx nest start gateway --watch"]
        ports:
        - containerPort: {{ .Values.gateway.service.targetPort }}
        envFrom:
          {{- toYaml .Values.envFrom | nindent 12 }}
        resources:
        #   {{- toYaml .Values.gateway.resources | nindent 10 }}
{{- end }}
